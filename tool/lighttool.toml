[tool]
id = "com.lightpass.reader"
label = "Passes"
versionCode = 2
versionName = "1.1.0"
permissions = ["android.permission.INTERNET"]
# Real device build talks to LightOS. Use
# "com.thelightphone.sdk.emulator" only for local emulator runs.
serverPackage = "com.lightos"
